// Parent Class
// If it's extended to child class and child class have display method. Only child classes method will execute if child class method
// has the same name. Using @Override or not doesn't matter
public class OverRideClass { // OverRideClass is the main class (parent)

    public void display(){
        System.out.println("Override Class with zero parameter");
    }
}
