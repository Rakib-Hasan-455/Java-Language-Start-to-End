// Parent Class
// If it's extended to child class and child class have display method. Only parent classes method will execute if child class method
// doesn't use @override annotation before the method
public class OverRideClass { 

    public void display(){
        System.out.println("Override Class with zero parameter");
    }
}
